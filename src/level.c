#include <stdlib.h>
#include <raylib.h>
#include "level.h"
#include "lib.h"

Level generateLevel(int floor, unsigned long seed) {
	floor = clamp(floor, 1, MAX_LEVEL);
	Level level = { };
	return level;
}
